<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN" monitorInterval="30">
    <Properties>
        <Property name="LOG_DIR">target/logs</Property>
        <Property name="LOG_PATTERN">%d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level [%X{testName}] %msg%n</Property>
        <Property name="ARCHIVE_PATTERN">${LOG_DIR}/archive/${date:yyyy-MM}/%d{yyyy-MM-dd}-%i.log</Property>
    </Properties>

    <Appenders>
        <!-- 1. Консольный аппендер с цветным выводом -->
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="%style{%d{HH:mm:ss.SSS}}{cyan} %highlight{%-5level} %style{[%X{testName}]}{yellow} %msg%n"/>
        </Console>

        <!-- 2. Файловый аппендер для каждого теста -->
        <Routing name="RoutingAppender">
            <Routes pattern="$${ctx:testName}">
                <Route>
                    <RollingFile name="TestLog-${ctx:testName}"
                                 fileName="${LOG_DIR}/${ctx:testName}.log"
                                 filePattern="${ARCHIVE_PATTERN}">
                        <PatternLayout pattern="${LOG_PATTERN}"/>
                        <Policies>
                            <SizeBasedTriggeringPolicy size="10 MB"/>
                        </Policies>
                        <DefaultRolloverStrategy max="20"/>
                    </RollingFile>
                </Route>
            </Routes>
        </Routing>
    </Appenders>

    <Loggers>
        <Root level="INFO">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="RoutingAppender"/>
        </Root>
    </Loggers>
</Configuration>